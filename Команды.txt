Print-Показать текст
abs- убирает минус
min- Ищет минимальное число в скобках
Max- Ищет максимальное число в скобках
Pow- Возводит в степь
Round- Округдяет до целого числа
input- Сохраняет переменные но заключает их в строку
int- Делает из числа которое не складывается складываемое число
float- Сохраняет число с точкой
if- Оператор если
elif- Тоже оператор после if
else - Это если 2 превыдущих не сработали